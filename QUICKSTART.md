# Быстрый старт ⚡

## За 5 минут до запуска

### 1️⃣ Установка зависимостей
```bash
cd /home/localadmin/uz-car-loan-calculator
npm install
```

### 2️⃣ Настройка окружения
```bash
# Скопируйте файл примера
cp .env.example .env

# Откройте файл для редактирования
nano .env
```

**Минимально необходимые настройки:**
```env
BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz  # От @BotFather
ADMIN_TELEGRAM_IDS=123456789                     # Ваш Telegram ID
APPLICATIONS_CHAT_ID=-100123456789               # ID группы для заявок
```

### 3️⃣ Получение токенов

**Telegram Bot Token:**
1. Откройте [@BotFather](https://t.me/BotFather)
2. Отправьте `/newbot`
3. Следуйте инструкциям
4. Скопируйте токен в `.env`

**Ваш Telegram ID:**
1. Откройте [@userinfobot](https://t.me/userinfobot)
2. Скопируйте ваш ID в `.env`

**Chat ID для заявок:**
1. Создайте новую группу в Telegram
2. Добавьте вашего бота в группу
3. Отправьте любое сообщение в группу
4. Откройте: `https://api.telegram.org/bot<BOT_TOKEN>/getUpdates`
5. Найдите `"chat":{"id":-100123456789}` и скопируйте в `.env`

### 4️⃣ Запуск приложения
```bash
npm run dev
```

Откройте:
- **Клиент:** http://localhost:3001
- **API:** http://localhost:3000

### 5️⃣ Настройка Mini App в Telegram

1. Откройте [@BotFather](https://t.me/BotFather)
2. Отправьте `/mybots`
3. Выберите вашего бота
4. `Bot Settings` → `Menu Button` → `Configure Menu Button`
5. Введите URL: `http://localhost:3001` (для локального тестирования используйте ngrok)
6. Введите текст: `Калькулятор 🚗`

### 6️⃣ Тестирование через ngrok (для Mini App)

```bash
# Установите ngrok
curl -s https://ngrok-agent.s3.amazonaws.com/ngrok.asc | sudo tee /etc/apt/trusted.gpg.d/ngrok.asc >/dev/null
echo "deb https://ngrok-agent.s3.amazonaws.com buster main" | sudo tee /etc/apt/sources.list.d/ngrok.list
sudo apt update && sudo apt install ngrok

# Авторизуйтесь (создайте аккаунт на ngrok.com)
ngrok config add-authtoken <your_token>

# Запустите туннель
ngrok http 3001
```

Используйте HTTPS URL от ngrok в настройках Menu Button.

---

## Проверка работоспособности ✅

### Проверка API
```bash
# Проверка здоровья сервера
curl http://localhost:3000/api/health

# Проверка курсов валют
curl http://localhost:3000/api/currency

# Проверка банков
curl http://localhost:3000/api/banks
```

### Проверка расчета
```bash
curl -X POST http://localhost:3000/api/calculate \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Тест",
    "username": "test",
    "carPrice": 200000000,
    "downPayment": 40000000,
    "currency": "UZS",
    "loanTerm": 36,
    "isWorking": true
  }'
```

### Проверка Telegram бота
1. Откройте бота в Telegram
2. Отправьте команду `/bank` (если вы админ)
3. Должна появиться конфигурация банков

---

## Структура проекта 📁

```
uz-car-loan-calculator/
├── client/                    # Next.js приложение
│   ├── app/
│   │   ├── page.tsx          # Главная страница
│   │   ├── layout.tsx        # Layout с Telegram SDK
│   │   └── globals.css       # Glassmorphism стили
│   ├── components/
│   │   ├── LoanForm.tsx      # Форма ввода данных
│   │   ├── ResultsView.tsx   # Результаты расчета
│   │   └── BankCard.tsx      # Карточка банка
│   └── types/
│       └── telegram-web-app.d.ts  # TypeScript типы
│
├── server/                    # Express API
│   ├── src/
│   │   ├── index.js          # Главный файл сервера
│   │   ├── services/
│   │   │   ├── calculator.service.js   # Расчет кредитов
│   │   │   ├── currency.service.js     # Курсы валют (ЦБ РУз)
│   │   │   └── telegram.service.js     # Telegram бот
│   │   └── utils/
│   │       └── logger.js     # Winston логирование
│   └── banks.config.json     # Конфигурация банков
│
├── .env                       # Переменные окружения
├── README.md                  # Общая документация
├── DEPLOYMENT.md              # Руководство по деплою
├── IMPROVEMENTS.md            # Предложения по улучшению
└── package.json               # Root package.json (workspaces)
```

---

## Основные команды 🛠

### Разработка
```bash
npm run dev          # Запуск клиента и сервера
npm run dev:client   # Только клиент (port 3001)
npm run dev:server   # Только сервер (port 3000)
```

### Продакшн
```bash
npm run build        # Сборка проекта
npm run start        # Запуск в продакшн режиме
```

### Git
```bash
git add .
git commit -m "Your message"
git push origin master
```

---

## Частые вопросы ❓

**Q: Приложение не запускается**
- Проверьте, что Node.js >= 18.0.0: `node --version`
- Убедитесь, что все зависимости установлены: `npm install`
- Проверьте, что порты 3000 и 3001 свободны

**Q: Telegram Mini App не открывается**
- Убедитесь, что используете HTTPS (через ngrok для локального тестирования)
- Проверьте, что URL корректно настроен в BotFather
- Проверьте логи клиента в браузере (F12)

**Q: Курсы валют не загружаются**
- Проверьте доступность API ЦБ РУз: `curl https://cbu.uz/ru/arkhiv-kursov-valyut/json/`
- Проверьте логи сервера
- Подождите 12 часов для обновления кэша или перезапустите сервер

**Q: Заявки не приходят в Telegram**
- Убедитесь, что бот добавлен в группу
- Проверьте APPLICATIONS_CHAT_ID в .env
- Убедитесь, что бот имеет права на отправку сообщений
- Проверьте логи сервера на ошибки

---

## Следующие шаги 🚀

После успешного запуска:

1. **Тестирование:**
   - Протестируйте все функции локально
   - Проверьте расчеты для разных сценариев
   - Убедитесь, что заявки приходят в Telegram

2. **Настройка банков:**
   - Обновите конфигурацию в `server/banks.config.json`
   - Добавьте реальные номера телефонов
   - Проверьте актуальность процентных ставок

3. **Деплой:**
   - Создайте аккаунт на [Railway](https://railway.app) или [Vercel](https://vercel.com)
   - Следуйте инструкциям в [DEPLOYMENT.md](./DEPLOYMENT.md)
   - Обновите Menu Button URL на продакшн URL

4. **Мониторинг:**
   - Следите за логами приложения
   - Собирайте обратную связь от пользователей
   - Планируйте улучшения (см. [IMPROVEMENTS.md](./IMPROVEMENTS.md))

---

## Поддержка 💬

Возникли проблемы? Проверьте:
- [README.md](./README.md) - полная документация
- [DEPLOYMENT.md](./DEPLOYMENT.md) - руководство по деплою
- [IMPROVEMENTS.md](./IMPROVEMENTS.md) - идеи для улучшения
- Логи приложения в консоли
- [Telegram Bot API документация](https://core.telegram.org/bots/api)

---

**Готово! 🎉** Приложение запущено и готово к использованию!
