# Руководство по развертыванию 🚀

## Локальная разработка

### 1. Установка зависимостей

```bash
npm install
```

### 2. Настройка переменных окружения

Скопируйте `.env.example` в `.env` и заполните необходимые значения:

```bash
cp .env.example .env
```

Обязательные переменные:
- `BOT_TOKEN` - токен вашего Telegram бота (получить от [@BotFather](https://t.me/BotFather))
- `ADMIN_TELEGRAM_IDS` - ID администраторов через запятую
- `APPLICATIONS_CHAT_ID` - ID чата для получения заявок

### 3. Создание Telegram бота

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям для создания бота
4. Скопируйте токен и добавьте в `.env`

### 4. Настройка Mini App

1. Отправьте команду `/mybots` в BotFather
2. Выберите вашего бота
3. Нажмите `Bot Settings` → `Menu Button` → `Configure Menu Button`
4. Введите URL вашего приложения (для локальной разработки используйте [ngrok](https://ngrok.com))
5. Введите текст кнопки: "Калькулятор 🚗"

### 5. Получение Chat ID

Для получения ID чата для заявок:

```bash
# 1. Добавьте бота в группу
# 2. Отправьте любое сообщение в группу
# 3. Откройте в браузере:
https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getUpdates

# 4. Найдите "chat":{"id": -100123456789} и скопируйте это значение
```

### 6. Получение Admin ID

Ваш Telegram ID можно получить через [@userinfobot](https://t.me/userinfobot)

### 7. Запуск приложения

```bash
# Запуск в режиме разработки (сервер + клиент одновременно)
npm run dev

# Или запуск отдельно:
npm run dev:server  # Запуск только сервера (порт 3000)
npm run dev:client  # Запуск только клиента (порт 3001)
```

Приложение будет доступно:
- Клиент: http://localhost:3001
- Сервер API: http://localhost:3000

### 8. Тестирование через ngrok

Для локального тестирования Telegram Mini App:

```bash
# Установите ngrok
brew install ngrok  # macOS
# или скачайте с https://ngrok.com

# Запустите туннель
ngrok http 3001

# Используйте HTTPS URL от ngrok в настройках Mini App
```

---

## Продакшн развертывание

### Рекомендуемые платформы

#### Railway (Рекомендуется)

1. Создайте аккаунт на [Railway](https://railway.app)
2. Нажмите `New Project` → `Deploy from GitHub repo`
3. Выберите ваш репозиторий
4. Railway автоматически определит монорепо структуру

Настройте переменные окружения в Railway:
```
NODE_ENV=production
PORT=3000
BOT_TOKEN=your_bot_token
ADMIN_TELEGRAM_IDS=123456789
APPLICATIONS_CHAT_ID=-100123456789
ALLOWED_ORIGINS=https://web.telegram.org
WEB_APP_URL=your-app.up.railway.app
```

#### Vercel (для клиента) + Railway (для сервера)

**Клиент на Vercel:**
```bash
cd client
vercel --prod
```

Переменные окружения в Vercel:
```
NEXT_PUBLIC_API_URL=https://your-server.up.railway.app
```

**Сервер на Railway:**
Следуйте инструкциям выше, но деплойте только папку `server`

#### Render

1. Создайте новый Web Service
2. Build Command: `npm install && npm run build`
3. Start Command: `npm run start`
4. Добавьте переменные окружения

---

## Обновление конфигурации банков

### Через Telegram команду (для админов)

```
/bank
```

Эта команда покажет текущую конфигурацию всех банков.

### Прямое редактирование

Отредактируйте `server/banks.config.json` и перезапустите сервер.

---

## Мониторинг и логирование

Логи сервера автоматически сохраняются в:
- Консоль (в режиме разработки)
- Файлы в `server/logs/` (в продакшене)

Уровни логирования:
- `error` - критические ошибки
- `warn` - предупреждения
- `info` - информация (по умолчанию)
- `debug` - отладка

Настройте через переменную `LOG_LEVEL` в `.env`

---

## Обновление курсов валют

Курсы валют автоматически обновляются каждые 12 часов из API ЦБ РУз.

Кэш можно настроить через:
```
CURRENCY_CACHE_HOURS=12
```

Для принудительного обновления перезапустите сервер.

---

## Безопасность

### Рекомендации для продакшена:

1. **Не коммитьте .env файл** - он уже в .gitignore
2. **Ограничьте CORS** - укажите только разрешенные домены
3. **Используйте HTTPS** - обязательно для Telegram Mini App
4. **Валидация данных** - уже реализована на сервере
5. **Rate limiting** - рекомендуется добавить для API endpoints

### Защита API endpoints

Добавьте rate limiting (опционально):

```bash
npm install express-rate-limit
```

```javascript
const rateLimit = require('express-rate-limit')

const limiter = rateLimit({
  windowMs: 15 * 60 * 1000, // 15 минут
  max: 100 // 100 запросов с одного IP
})

app.use('/api/', limiter)
```

---

## Troubleshooting

### Проблема: "Telegram WebApp is not defined"

**Решение:** Убедитесь, что скрипт Telegram загружается в `<head>`:
```html
<script src="https://telegram.org/js/telegram-web-app.js"></script>
```

### Проблема: Курсы валют не обновляются

**Решение:**
1. Проверьте доступность API ЦБ РУз
2. Проверьте логи сервера
3. Перезапустите сервер для сброса кэша

### Проблема: Заявки не приходят в Telegram

**Решение:**
1. Проверьте `APPLICATIONS_CHAT_ID` в .env
2. Убедитесь, что бот добавлен в группу и имеет права на отправку сообщений
3. Проверьте логи сервера на ошибки

### Проблема: Команда /bank не работает

**Решение:**
1. Проверьте, что ваш Telegram ID указан в `ADMIN_TELEGRAM_IDS`
2. Перезапустите сервер
3. Отправьте команду боту в личные сообщения

---

## Дополнительные возможности

### Добавление новых банков

Отредактируйте `server/banks.config.json`:

```json
{
  "id": "newbank",
  "name": "Новый Банк",
  "description": "Описание банка",
  "logo": "🏦",
  "baseRate": {
    "working": 18.0,
    "nonWorking": 22.0
  },
  "features": [
    "Быстрое одобрение",
    "Без справок"
  ],
  "contactPhone": "+998 XX XXX-XX-XX",
  "website": "https://newbank.uz",
  "conditions": {
    "minDownPaymentPercent": 20,
    "maxTermMonths": 60,
    "minAge": 21,
    "maxAge": 65
  }
}
```

### Интеграция с реальными банками

Для интеграции с реальными API банков:

1. Создайте новый сервис в `server/src/services/bank-integration.service.js`
2. Реализуйте методы для отправки заявок в API банков
3. Обновите `calculator.service.js` для использования реальных ставок
4. Добавьте обработку коллбэков от банков

---

## Поддержка

Если возникли вопросы:
- Проверьте логи приложения
- Создайте issue в GitHub репозитории
- Обратитесь к документации [Telegram Bot API](https://core.telegram.org/bots/api)

---

**Готово! 🎉** Ваше приложение готово к использованию!
